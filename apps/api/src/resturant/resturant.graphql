type Resturant {
  # Example field (placeholder)
  exampleField: Int
}

input CreateResturantInput {
  # Example field (placeholder)
  exampleField: Int
}

input UpdateResturantInput {
  id: Int!
}

type Query {
  resturant: [Resturant]!
  resturant(id: Int!): Resturant
}

type Mutation {
  createResturant(createResturantInput: CreateResturantInput!): Resturant!
  updateResturant(updateResturantInput: UpdateResturantInput!): Resturant!
  removeResturant(id: Int!): Resturant
}
